# Optional: enables natural-language generation on top of grounded dataset retrieval.
# The chatbot still works without a key using deterministic NLP + retrieval.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173
